#include <string>
#include <vector>

#ifndef INCLUDED_UTILS
#define INCLUDED_UTILS

bool TwoCellRootSiteQ(const int* s); // Determine if site is the root-site of a two-cell
bool TwoCellBoundaryLinkQ(int, const int*); // Determine if link lies on two-cell boundary


#endif
